<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.earsensei" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EarSensei" >
        <activity android:name=".ProfileActivity" >
        </activity>
        <activity android:name=".MusicTheoryActivity" />
        <activity android:name=".ChordsActivity" />
        <activity android:name=".ScalesActivity" />
        <activity android:name=".PerfectPitchActivity" />
        <activity android:name=".IntervalsActivity" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>