<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EarSensei">
        <activity
            android:name=".activities.StatsMenuActivity"
            android:exported="false" />
        <activity
            android:name=".activities.StatsDetailsActivity"
            android:exported="false" />
        <activity android:name=".activities.IntervalsSettingsActivity" />
        <activity android:name=".activities.StatsGeneralActivity" />
        <activity android:name=".activities.MusicTheoryActivity" />
        <activity android:name=".activities.quizactivities.ChordsQuizActivity" />
        <activity android:name=".activities.quizactivities.ScalesQuizActivity" />
        <activity android:name=".activities.quizactivities.PerfectPitchQuizActivity" />
        <activity android:name=".activities.quizactivities.IntervalsQuizActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>